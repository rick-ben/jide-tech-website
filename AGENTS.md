# 1. 先思考，再编码
**不做主观臆断，不掩饰疑惑，直面取舍利弊。**

动手开发前：
- 明确列出所有预设条件，存在不确定就主动提问。
- 若需求有多种解读方式，全部罗列出来，不要私自敲定一种。
- 如有更简洁的实现方案，如实提出；合理情况下敢于否决复杂方案。
- 遇到模糊不清的地方立刻停手，点明疑惑点并主动沟通。

# 2. 简洁优先原则
**用最少代码解决实际问题，不做多余的前瞻性设计。**

- 不额外开发需求以外的功能。
- 一次性使用的代码，不做过度抽象封装。
- 不凭空增加未要求的灵活性、可配置性。
- 不为不可能出现的场景做异常处理。
- 若写了200行代码就能用50行实现，务必重构精简。

自我审视：**资深工程师会不会觉得这段代码过于复杂？** 若会，立即简化。

# 3. 精准最小改动
**只修改必要代码，只清理自己造成的冗余问题。**

修改已有代码时：
- 不要顺手“优化”无关代码、注释和代码格式。
- 不重构原本无问题的逻辑。
- 遵循项目现有编码风格，即便自己习惯写法不同。
- 发现无关的废弃代码，只做备注说明，不要擅自删除。

若自身改动产生了无用代码：
- 删掉因**自己修改**变得无效的导入项、变量、函数。
- 非经允许，不得删除原本就存在的废弃代码。

检验标准：**每一行改动，都必须能直接对应业务需求。**

# 4. 目标导向执行
**明确验收标准，迭代优化直至验证通过。**

把模糊任务转化为可验证目标：
- “增加校验逻辑” → **为非法入参编写测试用例，再编码使用例通过**
- “修复漏洞bug” → **编写可复现bug的测试用例，再编码修复并通过**
- “重构模块X” → **确保重构前后所有测试用例均可正常通过**

多步骤任务需先列出简明执行计划：
```
1. 【步骤】→ 验证标准：【校验项】
2. 【步骤】→ 验证标准：【校验项】
3. 【步骤】→ 验证标准：【校验项】
```
